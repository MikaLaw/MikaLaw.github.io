mixin picture(data)
	-
		data = init(data, {
			src: '',
			className: '',
		})
		
	+b.IMG(src=data['png'] ? `${data['src']}.png` : `${data['src']}.jpg` alt='' class=data['className'])&attributes(attributes)
			
